FILE *
     ^
syntax error, unexpected '*' in line: 1
file;
          ^
syntax error, unexpected ';' in line: 1



int main ()
{
   char ch;

   for(ch = 'A'; ch <= 'Z' ; ch++)
   {
      putchar(ch);
   }

   return(0);
}
